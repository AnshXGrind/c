# Python ML Service Environment Variables

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=true

# CORS - Allow requests from Next.js frontend (Vercel)
# Add your Vercel deployment URL here
CORS_ORIGINS=http://localhost:3000,https://*.vercel.app

# Security
SECRET_KEY=your_super_secret_key_here_change_in_production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# File Upload
MAX_FILE_SIZE=10485760

# Optional: Database for persistence (not required for basic ML service)
# DATABASE_URL=postgresql://user:password@localhost:5432/careerai

# Optional: Cloud storage for files
# CLOUDINARY_CLOUD_NAME=your_cloud_name
# CLOUDINARY_API_KEY=your_api_key
# CLOUDINARY_API_SECRET=your_api_secret
